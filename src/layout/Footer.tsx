import { cn } from '@/utils/functions'

export const Footer = () => {
	return (
		<footer
			className={cn(
				'w-full p-5 flex items-center bg-slate-100 dark:bg-slate-800',
			)}
		>
			Footer
		</footer>
	)
}
